// Not import statement but import function call
import('./bootstrap');

// This files gets everything ready for bootstrap to load
// and guarantees that the productsIndex called by bootstrap
// will be available at go time.
